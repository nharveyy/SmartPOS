<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="oop.licao.smartpos.controller.ReceiptController">

    <!-- 🧭 STYLED HEADER BAR -->
    <top>
        <HBox spacing="15" alignment="CENTER_LEFT"
              style="-fx-background-color: #2c3e50; -fx-padding: 10;">
            <children>
                <Label text="SmartPOS"
                       style="-fx-text-fill: white; -fx-font-size: 18; -fx-font-weight: bold;"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button text="Continue Shopping"
                        onAction="#handleContinueShopping"
                        style="-fx-background-color: #3498db; -fx-text-fill: white;"/>
                <Button text="🚪 Logout"
                        onAction="#handleLogout"
                        style="-fx-background-color: #e74c3c; -fx-text-fill: white;"/>
            </children>
        </HBox>
    </top>

    <!-- 🧾 RECEIPT CONTENT -->
    <center>
        <VBox spacing="10" alignment="TOP_CENTER" style="-fx-padding: 30;">
            <children>
                <Label fx:id="invoiceLabel" text="INVOICE: " style="-fx-font-size: 16; -fx-font-weight: bold;"/>
                <Label fx:id="dateLabel" text="Date: "/>
                <Label fx:id="paymentLabel" text="Payment: "/>
                <Separator prefWidth="400"/>
                <VBox fx:id="itemsBox" spacing="5" style="-fx-padding: 10;"/>
                <Separator prefWidth="400"/>
                <Label fx:id="totalLabel" text="TOTAL: ₱0.00"
                       style="-fx-font-size: 16; -fx-font-weight: bold;"/>
                <Button text="Download Receipt"
                        onAction="#handleDownload"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white;"/>
            </children>
        </VBox>
    </center>
</BorderPane>
